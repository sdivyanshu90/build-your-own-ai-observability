apiVersion: v1
kind: ServiceAccount
metadata:
  name: aiobs
  namespace: aiobs
# The platform never calls the Kubernetes API. Not mounting the token removes a
# credential that would otherwise be available to anything that got code
# execution in the pod.
automountServiceAccountToken: false
